apiVersion: v1
kind: Service
metadata:
  name: api-aegis-ops-svc
  namespace: api-prod
spec:
  selector:
    app: api-aegis-ops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
